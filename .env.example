# Telegram Bot Token (from @BotFather)
BOT_TOKEN=your_bot_token_here

# Environment: development | production
NODE_ENV=production

# Webhook domain (production only, e.g. https://yourdomain.com)
WEBHOOK_DOMAIN=https://yourdomain.com

# Webhook path prefix
WEBHOOK_PATH=/webhook

# HTTPS port (production)
HTTPS_PORT=443

# HTTP port (development only)
PORT=3000

# Directory to store completed ZIP files (served via static)
DOWNLOADS_DIR=/app/downloads

# Public base URL for download links
DOWNLOAD_BASE_URL=https://yourdomain.com/downloads

# SSL certificate paths (production only)
SSL_CERT=/path/to/cert.pem
SSL_KEY=/path/to/key.pem

# Comma-separated list of allowed Telegram user IDs (leave empty to allow everyone)
# Example: ALLOWED_USERS=77933874,77933873
ALLOWED_USERS=
